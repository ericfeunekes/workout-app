---
title: Bugs — live tracker
status: living
purpose: Defects found in code — separate from open-questions.md (design debates) and feature docs (intended behavior).
covers:
  - whole project
---

# Bugs

This file tracks **defects** — code that does the wrong thing vs. spec, or things the spec promised and the code didn't deliver.

**Not here:**
- Unresolved design decisions → `docs/open-questions.md`
- Feature QA scenarios → `docs/features/<slug>.md`
- Architectural risk register → `docs/architecture/hotspots.md`

## Priority conventions

- **P0** — blocks v1 ship. Feature doesn't work or produces wrong data in the normal flow.
- **P1** — ship with workaround, fix before first real use. Visible but bypassable.
- **P2** — cosmetic, edge case, polish. Won't block Eric's first gym session.

## Status conventions

- `open` — identified, not yet started
- `in-progress` — someone's actively fixing
- `fixed` — commit merged + regression test + validation
- `wontfix` — acknowledged, deliberately not fixing (with reason)
- `deferred` — moved to v1.1+ (with reason)

---

## Active

### P0 — blocks v1

| ID | Title | Affected feature | Status | Notes |
|---|---|---|---|---|
| bug-007 | 9 of 11 timing-mode drivers are stubs | [timing-modes](features/timing-modes.md), [execute-loop](features/execute-loop.md) | `fixed` | All 11 modes registered in `DriverRegistry`. Round-based seeding + `BlockAdvancement` policy on `SessionState.Structure` drive mode-aware cursor advancement. VM-level `blockEndsAt` / `workEndsAt` + `tickBlockTimer()` handle time-capped modes (AMRAP / ForTime / EMOM / Tabata). See Fixed table. |
| bug-008 | Exercise-swap UI not built | [exercise-swap](features/exercise-swap.md) | `fixed` | Long-press on the ActiveView card fires a medium haptic + opens the new `SwapSheet` rendering alternatives from `WorkoutContext.alternativesByItem`. Tap commits via new `ExecutionViewModel.swap(itemID:, alternativeID:)`. Shell loads alternatives per item when building `WorkoutContext`. See Fixed table. |
| bug-009 | Alternative `parameter_overrides_json` never applied | [exercise-swap](features/exercise-swap.md) | `fixed` | New `CorePrescription.AlternativeOverrides.parse` reads `reps` / `load_kg` / `target_rir`. The `.swap` mutation carries the parsed overrides; the reducer mirrors reps/load onto non-done SetPlan rows and stores the struct on `SessionState.ItemLog.overrides` for `StraightSetsDriver` to shadow `target_rir` in its autoreg path. See Fixed table. |
| bug-010 | Past-set-edit has no server-push path | [past-set-edit](features/past-set-edit.md) | `fixed` | `enqueueEditedSet` now fires from `editPastSet`, and both `enqueueLoggedSet` and the new edit enqueue derive the `SetLog.id` deterministically from `(itemID, setIndex)` via `ExecutionViewModel.setLogID` (CryptoKit MD5 → `UUID(uuid:)`). Same id on original log + every edit → server upserts in place. See Fixed table. |
| bug-011 | Body-weight capture on completion not built | [save-and-done](features/save-and-done.md) | `fixed` | Decimal TextField on `CompleteView`; on `saveAndDone(bodyweightKg:)` a `UserParameter{key:"bodyweight_kg",source:.appLog}` fires through the new `onUserParameterChanged` hook → `PushQueue.enqueueUserParameter` → `POST /api/user-parameters`. Local cache upsert via `WorkoutCache.saveUserParameter`. Regression: `testSaveAndDoneEnqueuesBodyweightUserParameter`, `testSaveAndDoneNilBodyweightDoesNotFire`, Sync `PushQueue — userParameter routes to /api/user-parameters`. See Fixed table. |
| bug-012 | Workout note on completion not built | [save-and-done](features/save-and-done.md) | `fixed` | Multi-line TextField on `CompleteView`; on `saveAndDone(note:)` the trimmed note lands on `Workout.notes` in the local-cache write. Empty / whitespace-only collapses to `nil` via `ExecutionViewModel.normalizeNote`. Dictation-mic deferred (TODO in `saveAndDone` doc comment). Regression: `testSaveAndDoneWritesNoteToCompletedWorkout`, `testSaveAndDoneEmptyNoteCollapsesToNil`. See Fixed table. |
| bug-013 | Autoreg has no negative-load floor | [autoreg](features/autoreg.md) | `fixed` | `max(0.0, newLoad)` clamp in `Autoreg.propose`. Regression tests: "clamp · undershoot on prescribed=2.5" + "clamp · hit-failure on prescribed=4.0". 19/19 CoreAutoreg tests pass. |
| bug-014 | Smart defaults architecture — no way to author sparse prescriptions | cross-cutting | `fixed` | Shipped per ADR-2026-04-18. Migration `006_exercise_defaults.sql` adds library-default columns + raw-snapshot; server merges on POST/PUT /api/workouts. See Fixed table. |

### P1 — workaround exists or rarely hit

| ID | Title | Affected feature | Status | Notes |
|---|---|---|---|---|
| bug-015 | History session-detail set-row edit sheet stubbed | [history](features/history.md) | `fixed` | New `EditSetSheet` presents on row tap; commits route through a new `HistoryViewModel.editPastSet` that writes the updated SetLog locally (`WorkoutCache.saveSetLogs`), fires a shell-wired `onSetLogEdited` hook to `SyncAPI.pushLog` using the ORIGINAL log's UUID (server upserts in place), emits `history.past_set_edited` telemetry, and reloads so the row re-renders with the corrected values. See Fixed table. |
| bug-016 | Post-save History refresh relies on SwiftUI .task re-fire | [history](features/history.md), [save-and-done](features/save-and-done.md) | `fixed` | Two-part fix: (1) hoisted `historyVM` to `@State` on RootView so it's stable across body rebuilds (previously reconstructed each render — would lose filter state too). (2) added `afterLocalCompletion` hook to `AppBootstrap.bootstrap`; shell wires it to `historyVM.load()`. Debug-seed path uses same hook. |
| bug-017 | `editPastSet` doesn't emit telemetry | [past-set-edit](features/past-set-edit.md), telemetry | `fixed` | `emitPastSetEdited` fires a dedicated `execution.past_set_edited` event on every past-set edit, tagged with `workoutID` + `setLogID` and carrying `itemID` + `setIndex` in the data payload. See Fixed table. |
| bug-018 | FirstRun rapid double-tap re-entrancy | [firstrun](features/firstrun.md) | `fixed` | Guard at `FirstRunViewModel.connect()` early-returns when `state ∈ {.connecting, .syncingFirstPull, .complete}`; belt-and-braces with the view-side `.disabled(viewModel.isConnectInFlight)` binding. New named regression `testRapidDoubleTapConnectOnlyInvokesPipelineOnce` pins the scripted-transport contract (exactly one version + one pull — no duplicate saves or completions). |
| bug-019 | SwiftData `ModelContext.transaction` doesn't rollback on throw (iOS 17.x) | [persistence](features/persistence.md) | `wontfix (platform)` | Platform bug. Mitigated in app code via explicit do/catch/rollback pattern. Documented in `docs/architecture/hotspots.md`. Regression-tested by WorkoutCacheTests. |
| bug-030 | Server accepts malformed UUID strings | push-queue, smart-defaults, server | open | `_UuidNormalizingBase` lowercases `id`/`*_id` fields but doesn't validate UUID format. Posting `"id": "not-a-uuid"` to `/api/sync/results` returns 200 and inserts the bad string as primary key. Defense-in-depth issue — the iOS app generates valid UUIDs so not v1-blocking, but any malformed UUID Claude or a future client sends silently corrupts data. Fix: tighten the validator to reject non-UUIDs after lowercasing. **Found 2026-04-18 by ad-hoc QA.** |
| bug-033 | Server-side telemetry endpoint accepts unbounded batch sizes | telemetry, server | `fixed` | `TelemetryEventsPayload.events` now carries `Field(max_length=500)` on its Pydantic schema — 501+ events → 422 at ingest, zero writes. See Fixed table. Client-side 10k ring buffer still governs local retention; batches drain one event per `PushItem`, so the 500 cap is generous for normal flow. |
| bug-034 | Zero-item rest block at position 0 does not auto-advance on .start | execute-loop | `fixed` | `ExecutionViewModel.start()` already called `enterRestIfZeroItemBlock()` post-dispatch; the bug was speculative. New named regression `testStartOnZeroItemFirstBlockEntersRest` pins the invariant — `itemsPerBlock[0] == 0` → route is `.rest`, `restEndsAt` populated, NOT `.active`. Companion to the pre-existing `testRestBlockAtStartPositionEntersRestOnStart`. |
| bug-041 | POST /api/workouts not idempotent on duplicate id → 500, + PUT-replace strands items on some re-seeds | `server/workoutdb_server/api/workouts.py` | `fixed` | Both halves fixed in one cutover. (a) `create_workout` now detects when the caller supplies an `id` that already exists for this user and routes through the shared `_apply_workout_update` helper (same tree-replace semantics as PUT) — upserts in place, 200 OK, no PK crash. A supplied `id` owned by a different user → 404 (tenant isolation preserved). (b) `_apply_workout_update` replaces the blocks tree via `workout.blocks = []` + `db.flush()` + re-assign, forcing orphan-cleanup to complete before the same-PK re-inserts land. Eliminates the stranded-`blocks: []` race the composition-matrix seeder hit. Both PUT and POST-with-existing-id use this path — the two-phase PUT workaround in `scratch/e2e/seed_composition_matrix.py` is now obsolete. Regression tests: `test_post_workouts_repost_with_existing_id_upserts`, `test_post_workouts_repost_replaces_blocks`, `test_post_workouts_repost_rejects_another_users_id`. See Fixed table. |

### P2 — cosmetic / polish

| ID | Title | Affected feature | Status | Notes |
|---|---|---|---|---|
| bug-020 | Session detail set indexes render "2..N" instead of "1..N" | [history](features/history.md) | `fixed` | `formatSetRow` now uses `setIndex` as-is. Runtime pipeline is 1-based throughout (SessionSeeder cursor starts at 1). Updated HistoryViewModel fixture to match runtime convention. 9/9 History tests pass. |
| bug-021 | "JUST LOGGED" sub-labels too small for gym use | UX | `fixed` | New `DSTypography.subLabel` (12pt medium mono) replaces the prior 9pt. `DSPill` + "JUST LOGGED" group label both pull from the token. See Fixed table. |
| bug-022 | Active screen — weight dominates, set counter tiny | UX | `fixed` | New `DSTypography.subtitle` (14pt semibold sans) sits between `caption` and `body`. ActiveView's meta line ("SET 1 OF 4 · REST 0:15") + RestView's per-exercise sub-header now use it. Hero load stays at 64pt mono; hierarchy rebalanced by bumping the sub-header. See Fixed table. |
| bug-023 | Logging friction — 3 screens per set × 13 sets = 39+ taps | UX | `fixed` | New `LogSetSheet` collapses reps numpad + RIR row into one sheet with the keypad's inline "log" key as commit. NumPadSheet + RirSheet retained for single-field past-set edits on Rest. See Fixed table. |
| bug-024 | Numpad has no inline "done" | UX | `fixed` | `DSKeypad` gained optional `onDone:` + `doneLabel:` parameters; a full-width accent-colored button renders beneath the digit grid when wired. `LogSetSheet` now wires it with label "log" — bug-023 completes the caller wiring. |
| bug-025 | Sheet animation hitch when RIR picker slides up | UX | `fixed` | Primary mitigation: the logSet flow no longer presents a second sheet on top of the first (bug-023 rewrite). Remaining sheets (RirSheet, LogSetSheet) got explicit `.transition(.move(edge: .bottom))` so SwiftUI runs a single directional transition instead of composite fade + slide. See Fixed table. |
| bug-026 | Corner radii inconsistent between cards and buttons | design | `fixed` | New `DSRadius` token in `DesignSystem/Tokens/Radius.swift` centralizes the scale (pill=10, control=14, button=16, card=16, sheet=24). Cards now match buttons at 16. DSCard, DSPill, DSKeypad, DSButton all pull from the tokens. Visual diff pending but the scale is unified at the primitive level. |
| bug-027 | Mono numerics don't align with sans headers on weight | design | `fixed` | New `DSWeightLabel` primitive renders number + unit in the same mono family and weight; unit sits at ~0.55× the number face. Wired into ActiveView hero, RestView autoreg banner, and CompleteView ledger summary. See Fixed table. |
| bug-028 | Margin inconsistency on workout summary cards | design | `fixed` | CompleteView ledger rows render inside `DSCard(padding: 0)` with explicit 16pt horizontal / 12pt vertical inner rhythm. Exercise text gets the horizontal breath visual QA asked for. See Fixed table. |
| bug-029 | Tab bar accessibility IDs missing | QA | `deferred (v1.1+)` | SwiftUI TabView doesn't propagate accessibilityIdentifier. Workaround: coordinate-tap. Real fix needs custom tab bar. Watchlist in open-questions.md. |

---

## Fixed — 2026-04-18 session

All fixes include a regression test unless noted.

| ID | Title | Commit / file | Fix | Regression test |
|---|---|---|---|---|
| bug-001 | `HistoryRow` Button inside NavigationLink ate taps | `Packages/Features/History/.../HistoryRow.swift` | Flattened row to plain VStack; outer NavigationLink owns the tap | _None yet — need unit or UI test_ |
| bug-002 | Server emitted naive datetimes; Swift decoder silent-fails; bootstrap went `.empty` | `server/workoutdb_server/api/schemas.py` | `UtcDatetime` PlainSerializer; every Read-side `datetime` retyped | `tests/server/test_api_*` pass; FixtureDecodingTests |
| bug-003 | `executionVMHolder` weak capture — "start workout" silent no-op on prod path | `app/WorkoutDB/WorkoutDBApp.swift` | Strong capture — holder retained via `.ready` phase | _None yet — needs shell-level integration test_ |
| bug-004 | `/api/sync/results` 404'd on UUID case mismatch (Swift uppercase vs server lowercase) | `server/workoutdb_server/api/schemas.py` | `_UuidNormalizingBase` mixin; all Pydantic schemas inherit | Server pytest suite passes |
| bug-005 | `saveAndDone` didn't enqueue status_update on auto-advance path | `Packages/Features/Execution/.../ExecutionViewModel.swift` | saveAndDone now calls enqueueStatusCompleted | `testSaveAndDoneEnqueuesStatusExactlyOnce` |
| bug-006 | `complete()` + `saveAndDone()` double-enqueue status (regression from bug-005 fix) | same | Removed enqueue from complete(); saveAndDone is sole terminal enqueue | `testCompleteAloneDoesNotEnqueueStatus` + `testSaveAndDoneEnqueuesStatusExactlyOnce` |
| bug-011 | Body-weight capture on completion not built | `app/Packages/Features/Execution/.../CompleteView.swift` + `ExecutionViewModel.swift` + `ExecutionViewModel+Push.swift`; `app/Packages/Sync/.../PushQueue.swift` + `SyncAPI.swift` + `DTOMapping+UserParameter.swift`; `app/Packages/Persistence/.../WorkoutCache.swift` + `WorkoutCache+History.swift` + `PushQueuePayloadCoding*.swift`; `app/Packages/Shell/.../AppBootstrap+Hooks.swift` | New `.userParameter` case on `PushItem.Payload` routes to `/api/user-parameters`. `ExecutionPushHooks.onUserParameterChanged` wired by Shell to upsert locally + enqueue the POST. `CompleteView` renders a decimal TextField; on save the value is parsed to Double and passed into `saveAndDone(bodyweightKg:)`. | `testSaveAndDoneEnqueuesBodyweightUserParameter`, `testSaveAndDoneNilBodyweightDoesNotFire` (FeaturesExecution); `PushQueue — userParameter routes to /api/user-parameters` (SyncTests); `test_append_accepts_app_shaped_bodyweight_payload` (server) |
| bug-012 | Workout note on completion not built | `app/Packages/Features/Execution/.../CompleteView.swift` + `ExecutionViewModel.swift` + `ExecutionViewModel+Push.swift` | `CompleteView` renders a multi-line TextField. `saveAndDone(note:)` normalizes the note (trim + empty-collapse) and writes it onto the completed `Workout.notes` in the local-cache write. Dictation-mic punted with a TODO in the saveAndDone doc comment — tracked as a polish item. | `testSaveAndDoneWritesNoteToCompletedWorkout`, `testSaveAndDoneEmptyNoteCollapsesToNil` (FeaturesExecution) |
| bug-007 | 9 of 11 timing-mode drivers are stubs | `app/Packages/Features/Execution/Sources/FeaturesExecution/ExecutionViewModel.swift` (DriverRegistry), `SessionSeeder.swift` + `SessionSeeder+RoundBased.swift` (round-based seeding), `app/Packages/Core/Session/Sources/CoreSession/SessionState.swift` (`BlockAdvancement` on Structure + `blockEndsAt` / `workEndsAt`), `SessionReducer+Handlers.swift` (`nextCursor` branches on `advancementByBlock`), `ExecutionViewModel+Persistence.swift` (`enterBlockTimerIfNeeded`, `enterTabataWorkWindowIfNeeded`, `tickBlockTimer`, `autoLogAndRestForTabata`), `SessionStateCodable.swift` (persists the new timers + advancement). | All 11 drivers registered. Seeder is mode-aware: circuit/superset/forTime seed `block.rounds` rows per item; tabata 8; intervals `interval_count`; continuous 1; amrap/emom a 100-round sentinel (the wall clock terminates). Reducer's `nextCursor` picks `setMajor` vs `roundRobin` per block. VM owns time-cap timers (`blockEndsAt` for AMRAP / ForTime / EMOM / Tabata; `workEndsAt` for Tabata's 20s work window) and dispatches `.complete` / auto-logs a placeholder via `tickBlockTimer`. Design calls: AMRAP/EMOM seeded at 100 rows (simplest), Tabata work-expiry logs `(reps: 0, rir: nil)` — real per-round count deferred. | `testAMRAPBlockCompletesAtTimeCap`, `testEMOMCursorRoundRobinsPerInterval`, `testCircuitRoundsWalkItemsThenRoundBumps`, `testForTimeRoundSchemeRendersEachRoundReps`, `testContinuousSingleItemCompletesAfterLog` (FeaturesExecution); 6 new cases in `CoreSessionTests/main.swift` covering round-robin advancement + timer clearing on block change. |
| bug-014 | Smart defaults — Claude authoring was ~30 lines per item | `server/db/migrations/006_exercise_defaults.sql`, `server/workoutdb_server/sync/prescription_merge.py`, `server/workoutdb_server/api/workouts.py`, `server/workoutdb_server/api/exercises.py`, `schema/Sources/WorkoutDBSchema/Entities.swift`, `app/Packages/Core/Domain/Sources/CoreDomain/{Exercise,WorkoutItem}.swift`, `app/Packages/Sync/Sources/Sync/DTOMapping+{Exercise,Block}.swift`, `app/Packages/Persistence/Sources/Persistence/{SwiftDataModels,DomainMapping}.swift` | Exercise-library defaults (`default_prescription_json`, `default_alternatives_json`) merged server-side into each workout_item at ingest. Resolved blob is immutable; raw sparse payload preserved in `prescription_json_raw` when the merge changed anything. | `tests/server/test_prescription_merge.py` (14 cases covering library-only / item-only / nested autoreg / alternatives fallback); `tests/server/test_api_workouts.py` smart-defaults cases (resolve, alt fallback, no-op, repost idempotency, library-mutation stability, PUT re-merge); `tests/server/test_api_exercises.py` upsert-with-defaults; schema `CodableRoundTripTests` + `FixtureDecodingTests` cover the new fields; contract `test_fixture_decoding.py` adds `exercise_with_defaults.json`. |
| bug-010 | Past-set-edit has no server-push path | `app/Packages/Features/Execution/Sources/FeaturesExecution/ExecutionViewModel.swift` (`editPastSet` now dispatches side effects), `app/Packages/Features/Execution/Sources/FeaturesExecution/ExecutionViewModel+Push.swift` (new `enqueueEditedSet` + `handlePastSetEditSideEffects` + deterministic `setLogID`) | `enqueueLoggedSet` and `enqueueEditedSet` BOTH derive the pushed `SetLog.id` from `(itemID, setIndex)` via `Insecure.MD5` of a canonical `"uuid|index"` string fed into `UUID(uuid:)`. Same tuple → same UUID every call, so the original log push and every subsequent edit push carry the SAME id — server upserts in place. Design call: deterministic UUID (option B from the brief) instead of storing a setLogID on `SetPlan` — the latter would have collided with a parallel agent modifying `SessionState.ItemLog`. | `testEditPastSetEnqueuesSameUUIDAsOriginalLog`, `testEditPastSetEnqueuesWithUpdatedValues`, `testMultipleEditsOfSameSetAllUseSameUUID` (FeaturesExecution) |
| bug-017 | `editPastSet` doesn't emit telemetry | `app/Packages/Features/Execution/Sources/FeaturesExecution/ExecutionViewModel+Push.swift` (`emitPastSetEdited`, invoked from `handlePastSetEditSideEffects`) | Each past-set edit emits a dedicated `execution.past_set_edited` event (`kind: "state"`) tagged with the current `workoutID` and the deterministic `setLogID`, and carrying `{"itemID": "...", "setIndex": N, "setLogID": "..."}` in `data_json`. Downstream analysis can join the event back to the updated SetLog by id. | `testEditPastSetEmitsTelemetry` (FeaturesExecution) |
| bug-008 | Exercise-swap UI not built | `app/Packages/Features/Execution/Sources/FeaturesExecution/Sheets/SwapSheet.swift` (new), `ActiveView.swift` (`.onLongPressGesture` + haptic), `ExecutionViewModel.swift` (new `swap(itemID:alternativeID:)`), `WorkoutContext.swift` (`alternativesByItem`), `app/Packages/Shell/Sources/Shell/AppBootstrap.swift` (loads alternatives per item into the context) | Long-press on the Active screen's scroll card fires `UIImpactFeedbackGenerator(.medium)` + opens `SwapSheet`, a DSCard-styled list of alternatives with reason + "LAST · …" summary. Tap dispatches `ExecutionViewModel.swap` → reducer. Cancel is an explicit ghost button + the default drag-down. Empty-alternatives case renders a DSCard "no alternatives" placeholder. | `testSwapWithNoOverridesBehavesLikePureSwap`, `testSwapPreservesAlreadyDoneSets`, `testSwapEmitsTelemetry`, `testSwapWithUnknownAlternativeIsNoOp`, `testSwapIntegrationLogSetOneSwapThenSetTwoShowsOverrideLoad` (FeaturesExecution); `swap · target item gets performedExerciseID; other untouched; held preserved; logs preserved` (CoreSessionTests) |
| bug-009 | Alternative `parameter_overrides_json` never applied | `app/Packages/Core/Prescription/Sources/CorePrescription/AlternativeOverrides.swift` (new parser), `app/Packages/Core/Session/Sources/CoreSession/SessionState.swift` (new `ItemLog.overrides`), `SessionMutation.swift` + `SessionReducer+Handlers.swift` (`.swap` carries overrides; `applyOverridesToSetPlans` mirrors reps/load onto non-done rows, manual rows preserved), `app/Packages/Features/Execution/Sources/FeaturesExecution/Drivers/StraightSetsDriver.swift` (autoreg path prefers `itemLog.overrides?.targetRir`), `SessionStateCodable.swift` (persists overrides) | `AlternativeOverrides.parse(json)` reads `reps` / `load_kg` / `target_rir`; empty / malformed JSON collapses to an empty struct (swap still proceeds). reps & load ride onto the remaining non-done `SetPlan` rows at swap time so the Active screen picks them up; `target_rir` lives on `ItemLog.overrides` and is read by `StraightSetsDriver` to shadow the authored `target_rir` in its autoreg path. Other override keys (autoreg step sizes, sets count, shape morph) are deferred with a TODO in the parser header. | `testSwapAppliesLoadOverrideToRemainingSets`, `testSwapAppliesRepsOverrideToRemainingSets`, `testSwapTargetRirOverrideShadowsPrescription`, `testSwapWithoutTargetRirOverrideRetainsPrescriptionBehavior` (FeaturesExecution); `swap+overrides · remaining sets updated; logged set preserved; target_rir stored`, `swap+empty overrides · overrides field stays nil`, `swap+overrides · manual set preserved, other remaining sets overridden` (CoreSessionTests) |
| bug-031 | Bug-030 fix broke sync_pull for any non-UUID exercise_id in DB | `server/workoutdb_server/api/schemas.py` | Split `_UuidNormalizingBase` into `_UuidInputBase` (lowercase + UUID-format validation, for Create/Update/Upsert/sync-in payloads) and `_UuidReadBase` (lowercase only, trusts the DB, for Read-side ORM → Pydantic conversion). The DB is authoritative on egress — a strict validator there 500'd `sync_pull` the moment any legacy or seed row appeared. Input-side validation (bug-030 guard) retained. | `test_sync_pull_tolerates_legacy_non_uuid_exercise_id` (seeds `Exercise(id="ex-0")` and asserts 200 + id round-trips unchanged); `test_sync_pull_uses_bounded_queries` (perf) green again; `test_sync_results_rejects_malformed_uuid` + `test_sync_results_rejects_malformed_workout_item_id` still green (input-side guard intact) |
| bug-032 | Server stores unparseable prescription/alternatives JSON, later crashes 500 | `server/workoutdb_server/api/schemas.py` | Added `field_validator` on `ExerciseUpsert.default_prescription_json` and `default_alternatives_json` that runs `json.loads` and raises `ValueError` on parse failure → 422 at ingest. None still allowed (no defaults is a valid state). Deeper shape validation (object vs array, required keys) stays in `prescription_merge._load_or_empty` / `merge_alternatives` — the ingest validator only proves the blob is parseable, which is enough to prevent the downstream 500. | `test_post_exercises_rejects_malformed_default_prescription_json`, `test_post_exercises_accepts_valid_default_prescription_json`, `test_post_exercises_rejects_malformed_default_alternatives_json` |
| bug-021 | "JUST LOGGED" sub-labels too small for gym use | `app/Packages/DesignSystem/Sources/DesignSystem/Tokens/Typography.swift` (new `DSTypography.subLabel`), `Primitives/DSPill.swift`, `Packages/Features/Execution/Sources/FeaturesExecution/RestView.swift` | New `subLabel` token (12pt medium mono, tracking 1.2). `DSPill` captions + the "JUST LOGGED" group label both pull from it. The 9pt sub-labels (`.system(size: 9, design: .monospaced)`) were unreadable at arm's length in a gym; 12pt medium is the new floor for any uppercase mono sub-caption in the app. | Visual-only; DesignSystem smoke test `DSTypography exposes the full ramp` exercises the new token. |
| bug-022 | Active screen — weight dominates, set counter tiny | `app/Packages/DesignSystem/Sources/DesignSystem/Tokens/Typography.swift` (new `DSTypography.subtitle`), `Packages/Features/Execution/Sources/FeaturesExecution/ActiveView.swift`, `RestView.swift` | New `subtitle` token (14pt semibold sans) sits between `caption` (11pt) and `body` (15pt). ActiveView's "SET N OF M · REST mm:ss" + RestView's per-exercise sub-header both use it. Design call: kept the 64pt mono hero load; rebalanced hierarchy by bumping the sub-header instead of shrinking the hero. Caption (11pt) is retained for purely decorative kickers — anything the user must read to act on now lives on `subtitle`. | Visual-only; DesignSystem smoke test covers the token. |
| bug-023 | Logging friction — 3 screens per set × 13 sets = 39+ taps | `app/Packages/Features/Execution/Sources/FeaturesExecution/Sheets/LogSetSheet.swift` (new), `ActiveView.swift` (retire dual-sheet chain) | New `LogSetSheet` = single sheet = reps numpad at top, RIR row (0-5) between readout and keypad, `DSKeypad(onDone:)` wired as the commit ("log"). Pure-Swift `LogSetSheetModel` owns buffer + pickedRir + commit, so the contract is unit-testable without ViewInspector. NumPadSheet + RirSheet stay for single-field past-set edits on the Rest screen; the log flow on Active is the only caller that needed both fields at once. User can still commit with RIR nil (matches prior "skip" semantics); tapping the same RIR row twice toggles it off. | `LogSetSheetTests.testPrimeSeedsBufferWithInitialReps`, `testCommitWithRirUntouchedFiresNilRir`, `testCommitAfterPickingRirFiresBoth`, `testCommitAfterEditingRepsFiresEditedReps`, `testTappingSameRirClearsIt`, `testPickingDifferentRirReplacesPrevious`, `testPressDigitOverwritesZeroFirstSlot`, `testPressDigitAppendsAfterPrimedBuffer`, `testPrimeIsIdempotent`, `testCommitWithUnparseableBufferFallsBackToInitialReps` (10 new cases). |
| bug-025 | Sheet animation hitch when RIR picker slides up | `app/Packages/Features/Execution/Sources/FeaturesExecution/Sheets/RirSheet.swift`, `Sheets/LogSetSheet.swift` | Primary mitigation is the bug-023 rewrite — the logSet flow no longer presents sheet-over-sheet, so the stutter point where the RIR sheet slid up on top of a dismissing NumPad sheet is gone. Remaining sheets (RirSheet for past-set edits, the new LogSetSheet) got explicit `.transition(.move(edge: .bottom))` so SwiftUI runs a single directional transition on present — the default composite fade+slide ran a re-geometry on the first frame on some devices, which is the most likely source of the dropped frame in the img-ask capture. | Visual-only. The flow change (single sheet) is covered by the bug-023 unit tests. |
| bug-027 | Mono numerics don't align with sans headers on weight | `app/Packages/DesignSystem/Sources/DesignSystem/Primitives/DSWeightLabel.swift` (new), `Packages/Features/Execution/Sources/FeaturesExecution/ActiveView.swift` (hero load), `RestView+Banner.swift` (autoreg banner), `CompleteView+Ledger.swift` (ledger summary) | New `DSWeightLabel(number:unit:size:weight:color:)` primitive renders digits + unit in the same mono family and weight — unit sits at ~0.55× the number face on the same baseline. Replaces the prior `Text("102.5 kg")` string pattern, which in practice let "kg" fall through to the nearest sans font in the hierarchy. Design call: mono unit at reduced size (vs. tabular sans at matching weight) — the number is already mono, so making the unit sans required an explicit style break every time, and mono-matches-mono is the easier-to-reason-about pairing. | Visual-only; DesignSystem smoke test `DSWeightLabel constructs at hero and inline sizes` exercises construction. |
| bug-028 | Margin inconsistency on workout summary cards | `app/Packages/Features/Execution/Sources/FeaturesExecution/CompleteView.swift`, `CompleteView+Ledger.swift` (extracted ledger helpers) | Ledger rows switched from `DSCard { ... }` (uniform 16pt padding) to `DSCard(padding: 0) { ... }` with explicit `.padding(.horizontal, DSSpacing.xl)` + `.padding(.vertical, DSSpacing.lg)` — a 16×12 rhythm that gives exercise text room on the sides without ballooning the card height. Extracted `LedgerEntry` + `summary(for:)` + `ledgerSummaryView` into `CompleteView+Ledger.swift` so CompleteView stays under SwiftLint's `type_body_length` cap. Same split applied to `RestView` → `RestView+Banner.swift` in the bug-027 pass. | Visual-only. |
| bug-015 | History session-detail set-row edit sheet stubbed | `app/Packages/Features/History/Sources/FeaturesHistory/HistoryViewModel.swift` (+ `HistoryViewModel+Edit.swift` new, `EditSetSheet.swift` new, `HistorySessionDetailView.swift` rewritten, `SessionDetailViewModel.swift` gains `setLogsByID` + `workoutID`); `app/Packages/Features/History/Package.swift` adds `CoreTelemetry` dep; `app/Packages/Shell/Sources/Shell/AppBootstrap.swift` adds `historyViewModel:` param; `app/WorkoutDB/WorkoutDBApp.swift` wires it; `app/Packages/Features/History/Tests/FeaturesHistoryTests/FakeHistoryCache.swift` records `saveSetLogs` calls | Tap on a set row opens a new `EditSetSheet` (reps + RIR + load numpad in one sheet, single "save" commit). Commit flows through a new `HistoryViewModel.editPastSet(workoutID:setLogID:reps:rir:loadKg:)` that (a) mutates the cached SetLog by `id`, (b) calls `WorkoutCache.saveSetLogs([edited])` for local persistence, (c) emits `history.past_set_edited` telemetry, (d) fires the shell-wired `onSetLogEdited` hook to `SyncAPI.pushLog([log])` — the edited SetLog carries the ORIGINAL log's UUID so the server upserts in place (no duplicate row), and (e) reloads so the detail VM re-derives immediately. Design call: edits dispatch via a new VM method rather than the reducer path (History looks at completed workouts — SessionState has already been wiped by saveAndDone; there's no live reducer to target). | `testHistoryEditPastSetEnqueuesPush`, `testHistoryEditReloadsAfterCommit`, `testHistoryEditEmitsTelemetry` (FeaturesHistoryTests) |
| bug-018 | FirstRun rapid double-tap re-entrancy | `app/Packages/Features/FirstRun/Tests/FeaturesFirstRunTests/FirstRunViewModelTests.swift` (new test; code fix predated this slice) | The re-entrancy guard itself has been on `FirstRunViewModel.connect()` since an earlier session — when `state ∈ {.connecting, .syncingFirstPull, .complete}` the second call short-circuits before touching state / TokenStore / transport. Belt-and-braces view-side `.disabled(viewModel.isConnectInFlight)` on the connect button. This slice adds the named regression `testRapidDoubleTapConnectOnlyInvokesPipelineOnce` using a scripted-transport recorder that enqueues EXACTLY ONE happy path — if the guard regressed and a second call fell through, the transport would serve its 200-empty fallback and `transport.callCount` would jump from 2 to 4. | `testRapidDoubleTapConnectOnlyInvokesPipelineOnce` (FeaturesFirstRunTests) |
| bug-033 | Server-side telemetry endpoint accepts unbounded batch sizes | `server/workoutdb_server/api/schemas.py` (`TelemetryEventsPayload.events` adds `Field(max_length=500)`) | Pydantic enforces the cap at ingest — 501+ events → 422, zero writes, the DB is never touched. 500 is generous for normal flow: the client push queue drains one event per `PushItem.Payload.events([Event])` (per `docs/features/telemetry.md`), so a full 10k local ring buffer would take 20 requests to propagate, not one. Design call: cap at the Pydantic validator layer rather than the route handler — a field-level limit keeps the error shape uniform (standard 422 with `List should have at most 500 items`) and there's nothing to unit-test in Python besides "boundary rejected." | `test_telemetry_rejects_oversized_batch`, `test_telemetry_accepts_batch_at_cap` (tests/server/test_api_telemetry.py) |
| bug-034 | Zero-item rest block at position 0 does not auto-advance on .start | `app/Packages/Features/Execution/Tests/FeaturesExecutionTests/ExecutionViewModelTests.swift` (new test; no code change — bug was speculative) | The original bug report hypothesized `applyStart` needed to chain into `advanceCursor` for zero-item blocks, but the current implementation already handles it: `ExecutionViewModel.start()` dispatches `.start` (route → `.active`) and then calls `enterRestIfZeroItemBlock()`, which checks `itemsPerBlock[cursor.blockIndex] == 0` and dispatches `.enterRest(durationSec:now:)` with the `RestBlockDriver`'s `duration_sec`. The route ends up on `.rest` with `restEndsAt` populated — correct end-state, no rendering dead-end. Companion to `testRestBlockAtStartPositionEntersRestOnStart`, this test pins the bug-named invariant (route ≠ `.active`, restEndsAt = now + `duration_sec`) and the seeder's `itemsPerBlock = [0, 1]` precondition. | `testStartOnZeroItemFirstBlockEntersRest` (FeaturesExecutionTests) |

---

## How to use this

- **Starting work on a bug:** move it to `in-progress`, put your name/agent ID next to it.
- **Adding a bug:** next ID (`bug-035` next; `bug-030`–`bug-034` already assigned), pick priority, link affected feature doc.
- **Fixing a bug:** move to the fixed table, cite commit + test. If there's no regression test, note `_None yet_` — the fix isn't complete without one.
- **Demoting a feature's status:** file a bug here in the same commit that updates `docs/spec.md`.
- **Wontfix / deferred:** short rationale in Notes; link ADR or open-question if a design decision supports the call.

Cross-ref: `docs/spec.md` feature matrix reflects aggregated state; individual bugs here drive it down as they're found.

## Fixed post-review (bug-035, bug-036, bug-037)

| bug-035 | `LogSetSheetModel` missing `@Observable` — numpad taps didn't re-render | [execute-loop](features/execute-loop.md) | `fixed` | Original landing (bug-023 agent) left `LogSetSheetModel` as `final class`, which SwiftUI can't observe. Unit tests passed because they read `model.buffer` directly; runtime silently froze the reps readout at "0". Fix: `@Observable @MainActor public final class LogSetSheetModel`. Found by MCP-driven validation pass; regression covered implicitly by the existing `LogSetSheetTests` + the MCP validation recording. |
| bug-036 | Today tab doesn't advance to next workout after save & done | `app/Packages/Features/Today/Sources/FeaturesToday/TodayViewModel.swift`, `app/Packages/Shell/Sources/Shell/AppBootstrap.swift` + `AppBootstrap+Hooks.swift`, `app/WorkoutDB/RootView+DebugLaunch.swift` | `fixed` | Mirrors bug-016 pattern. `TodayViewModel`'s derived fields (`programName`, `programTags`, `lastSessionSummary`, `exercises`, `workoutID`) flip from `let` to `internal(set) var`; new `reload(using: TodayLoader)` re-runs the loader and calls `apply(_:)` to replace state in place. A loader return of `nil` flips `isEmpty = true` and blanks the fields so the shell renders the existing S8 empty-glance. `AppBootstrap.ReadyInputs` gains `todayLoader`; `makeCompletionWriter` captures `todayVM` + `todayLoader` and calls `todayVM.reload(using:)` AFTER the cache write (so the loader sees `.completed` and skips) and BEFORE `afterLocalCompletion` (History `load()`). Debug-seed path in `RootView+DebugLaunch.swift` mirrors the wiring for parity via a new `DebugExecutionHolder`. Regression tests: `testTodayViewModelReloadPicksNextPlannedAfterCompletion`, `testTodayViewModelReloadToEmptyWhenNoPlannedWorkouts` (FeaturesTodayTests). Found by MCP validation 2026-04-18. |
| bug-037 | AMRAP Active screen horizontally overflows (999 progress dots) | `app/Packages/Features/Execution/Sources/FeaturesExecution/Drivers/AMRAPDriver.swift`, `app/Packages/Features/Execution/Sources/FeaturesExecution/ActiveView.swift` | `AMRAPDriver` now passes `totalSets: 0` (via the new `AMRAPDriver.unboundedRoundsCount` public constant) to declare "no bound". `ActiveView` gates the progress-dot `ForEach` AND the meta line's "OF M" denominator on `totalSets > 0`; when unbounded, the meta line collapses to `ROUND N · REST mm:ss`. Extracted the two renderers (`formattedMetaLine`, `shouldRenderProgressPips`) onto `ActiveView` as pure statics so the contract is unit-testable without SwiftUI snapshotting. Design call A over B from the bug report: "progress dots never render at all for unbounded" is cleaner than "clamp to 20 and show …" — semantically, AMRAP has no progress denominator to draw, so no ring is the honest render. Reviewed other time-capped drivers (`EMOMDriver` up to `total_minutes` rows, `ForTimeDriver` 3-9 rounds, `TabataDriver` 8 rounds, `ContinuousDriver` 1, `Circuit`/`Superset` bounded by `block.rounds`) — all render budgets well within the ~20-dot comfort zone, no changes needed. If EMOM ever gets authored with pathological `total_minutes`, same fix applies: seed `totalSets = 0` on the driver. | `testAMRAPDriverTotalSetsIsZeroForUnboundedRounds`, `testUnboundedRoundsCountIsZero` (AMRAPDriverBug037Tests); 8 view-level cases in `ActiveViewMetaLineTests` covering bounded/unbounded rendering of both `formattedMetaLine` and `shouldRenderProgressPips`; pre-existing `AMRAPDriverTests.testActiveContentResolvesFirstItem` updated to assert `totalSets == 0`. |

## Fixed — bug-038 (Tabata rest timer state invariant)

| ID | Title | Commit / file | Fix | Regression test |
|---|---|---|---|---|
| bug-038 | Tabata rest timer renders 0:00 immediately on entering rest | `app/Packages/Features/Execution/Tests/FeaturesExecutionTests/ExecutionViewModelTests.swift` (new regression tests; no code change — bug was not reproducible at the state level) | Code audit + new FixedClock integration tests confirm the state-level contract is already correct: a manual `logSet` on a Tabata block dispatches `.enterRest(durationSec: 10, now: clock.now)` built via `buildLogMutations`, and the reducer stamps `restEndsAt = now + 10` + `route = .rest`. `TabataDriver.restDuration` is hardcoded to 10 per spec and doesn't branch on cursor. `enterBlockTimerIfNeeded`/`enterTabataWorkWindowIfNeeded` run AFTER apply and only touch `workEndsAt`/`blockEndsAt` — they never overwrite `restEndsAt`. Hypotheses (a) "enterRest with duration 0", (b) "restEndsAt in the past", and the underlying "driver sees post-log cursor" theory all failed to reproduce. Most likely the validation observation was a restored-from-disk stale state (session-store `restEndsAt` persisted from a prior run) or a transient TimelineView first-paint quirk. Fix posture: pin the state invariant with regression tests and treat bug-040 / bug-039 as the real fixes covering the adjacent flows. If the render-side repro surfaces again, add a TimelineView-level test (or snapshot). | `testTabataLogSetEntersRestWithTenSecondsRemaining` (FeaturesExecutionTests · ExecutionViewModelTests) — seeds a Tabata block, logs a set under FixedClock, asserts `state.route == .rest`, `state.restEndsAt == now + TabataDriver.restSec`, and `vm.restDurationSeconds == 10`. `testTabataRoundTwoLogSetRefreshesRestEndsAt` — after round-1 rest → advance → round-2 log, asserts `restEndsAt` is stamped against the CURRENT clock (not stale from round 1). |

## Fixed — bug-039 (straight_sets single-field timing config)

| ID | Title | Commit / file | Fix | Regression test |
|---|---|---|---|---|
| bug-039 | Straight-sets rest timer returns 0 when only `rest_between_sets_sec` set | `app/Packages/Core/Prescription/Sources/CorePrescription/PrescriptionParser+TimingConfig.swift` (`parseStraightSetsConfig`), `docs/prescription.md` | Switched both fields from `readRequiredDouble` to `readOptionalDouble`. The missing field defaults to the present one (`rbs = rbsRaw ?? rbeRaw ?? 0`, `rbe = rbeRaw ?? rbsRaw ?? 0`). Root cause: the parser's strict-both assertion failed the parse on `{"rest_between_sets_sec": 15}` and `StraightSetsDriver.restDuration`'s `.failure` branch returned 0, silently dropping the authored rest. Design call: symmetric fallback rather than "exercises defaults to sets only" — a config authored with ONLY `rest_between_exercises_sec` should also produce a functional between-sets rest. Semantically the two rests express the same user intent when only one is given. | `bug-039 · straight_sets · only rest_between_sets_sec → both fields equal`, `bug-039 · straight_sets · only rest_between_exercises_sec → both fields equal`, `bug-039 · straight_sets · both absent → both fields zero` (CorePrescriptionTests); `testRestDurationDefaultsExercisesToSetsWhenMissing` (FeaturesExecutionTests · StraightSetsDriverTests) |

## Fixed — bug-040 (History past-set edit duplicated set_log on the server)

| ID | Title | Commit / file | Fix | Regression test |
|---|---|---|---|---|
| bug-040 | History session-detail edit creates a duplicate set_log row on the server instead of upserting | `app/Packages/Features/Execution/Sources/FeaturesExecution/ExecutionViewModel+Push.swift` (`writeCompletionToLocalCache`) | True root cause was upstream of `HistoryViewModel.editPastSet`. The Execution `saveAndDone` path wrote completed set_logs to the **local** cache with a fresh `UUID()`, while `enqueueLoggedSet` pushed the same logical set to the **server** with a deterministic `setLogID(itemID:setIndex:)`. Local cache + server thus carried different ids for the same set. A later History edit correctly copied the CACHE's id — which didn't match any server row, so `sync_results` INSERTed a duplicate instead of updating in place. Fix: `writeCompletionToLocalCache` now stamps `SetLog.id = Self.setLogID(itemID:, setIndex:)`, matching the push path. One logical set → one UUID everywhere (local cache + server + telemetry). The existing `HistoryViewModel.editPastSet` reuse-by-id logic is correct and unchanged — it's now operating on the SAME id the server already has. Scenario S_HISTORY_IDEMPOTENT covered. Found by MCP E2E validation 2026-04-18. | `testSaveAndDoneLocalCacheUsesDeterministicSetLogID` (FeaturesExecutionTests · ExecutionViewModelTests) — asserts every SetLog written to the local cache by `saveAndDone` carries the deterministic id used on the push path; `testHistoryEditUsesExistingSetLogIdForIdempotentUpsert` (FeaturesHistoryTests · HistoryViewModelEditPastSetTests) — pins a known id `A1B2…5678` on the seeded log and asserts the push hook receives the ORIGINAL id, not a regenerated one. |

## Fixed — bug-042 (time-cap timers never fired at runtime)

| ID | Title | Commit / file | Fix | Regression test |
|---|---|---|---|---|
| bug-042 | AMRAP / ForTime / EMOM / Tabata time-cap + interval-tick auto-advance was dead code | `app/Packages/Features/Execution/Sources/FeaturesExecution/ActiveView.swift`, `RestView.swift`, `ExecutionViewModel.swift` (new `tickCallCount`), `ExecutionViewModel+Persistence.swift` (`tickBlockTimer` now increments the counter + doc comment cites the view wiring) | Both `ActiveView` and `RestView` now carry a `Timer.publish(every: 1, on: .main, in: .common).autoconnect()` publisher + `.onReceive(tickTimer)` that invokes `viewModel.tickBlockTimer()` once per second while `state.blockEndsAt != nil`. Design calls: (a) Combine `Timer.publish` over `TimelineView` because the tick must fire as a side effect (not a render read) — `TimelineView` is a render construct, the clean pattern for side effects is `.onReceive`. (b) Gated on `blockEndsAt != nil` as an optimization — straight_sets / superset / circuit / rest blocks don't wake the VM every second. Correctness doesn't depend on the gate (the VM's `tickBlockTimer` is a no-op when both `blockEndsAt` and `workEndsAt` are nil), but avoiding the wake keeps the battery story honest. (c) Rest view also carries the tick because block caps can elapse WHILE the user is resting — an EMOM's total_minutes expires during a between-interval rest, a For-Time cap hits while the user is still resting; the Active view's tick would otherwise go silent on route change. (d) Added a non-`@Observable` `tickCallCount: Int` to the VM for test observability — proves the tick handler wired to the real view will actually fire the VM method, without needing to boot a SwiftUI runtime in tests. | `testActiveViewInvokesTickBlockTimerOncePerSecond` (mirrors TimelineView firing by calling `tickBlockTimer()` three times across a moving `MutableTickClock`, asserts `tickCallCount >= 3` and that the cap elapsing flips the route to `.complete`); `testTickBlockTimerIsSafeWhenNoBlockEndsAt` (straight_sets block — 5 ticks, no state change, count still increments — guards the view gate as an optimization rather than a correctness requirement); `testActiveViewWiresTickBlockTimerViaPeriodicTimer` + `testRestViewWiresTickBlockTimerViaPeriodicTimer` (source-inspection tests — read the two view source files via `#filePath`, assert `Timer.publish(every: 1` / `.autoconnect()` / `.onReceive(tickTimer)` / `viewModel.tickBlockTimer()` / `state.blockEndsAt != nil` are all present; any refactor that drops the wiring trips these tests, not the runtime). Pre-existing `testAMRAPBlockCompletesAtTimeCap` still pins the underlying VM contract. |
