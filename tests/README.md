# Tests

See `docs/TESTING.md` for the proof contract.

## Layout (target)

- `server/` — Python tests for the home server (API, schema, sync). Initial focus.
- `contract/` — cross-stack contract tests once `schema/` is populated.

The iOS app's tests live alongside its Xcode target in `app/`.
